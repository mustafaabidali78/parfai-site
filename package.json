{
  "name": "parfai-site-analytics",
  "private": true,
  "type": "module",
  "description": "Scheduled analytics report generator for parfai.org — see reports/analytics/README.md. The site itself is plain static HTML/CSS/JS and has no build step; this package.json exists only for the scripts/ tooling.",
  "scripts": {
    "analytics-report": "node scripts/analytics-report.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2"
  }
}
