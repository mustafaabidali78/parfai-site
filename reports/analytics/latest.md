# ParfAI analytics report — not generated yet

This file is a placeholder. It will be overwritten by the first real run of
[`.github/workflows/analytics-report.yml`](../../.github/workflows/analytics-report.yml)
— either the next scheduled run (daily, 06:00 UTC) or one triggered manually
from the repo's **Actions** tab (**Analytics report** → **Run workflow**).

For full data (logins and feedback, not just signups and reviews), run the
one-time setup in [`supabase/README.md`](../../supabase/README.md) first.
